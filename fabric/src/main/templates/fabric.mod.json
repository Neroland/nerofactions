{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${mod_version}",
  "name": "${mod_name}",
  "description": "NeroFactions - multiloader build (Fabric).",
  "authors": ["${mod_authors}"],
  "license": "${mod_license}",
  "accessWidener": "nerofactions.accesswidener",
  "environment": "*",
  "entrypoints": {
    "main": ["za.co.neroland.nerofactions.fabric.NeroFactionsFabric"],
    "client": ["za.co.neroland.nerofactions.fabric.NeroFactionsFabricClient"]
  },
  "mixins": [
    "nerofactions-common.mixins.json",
    "nerofactions.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${fabric_loader_version}",
    "minecraft": ">=${minecraft_version}",
    "java": ">=21",
    "fabric-api": "*"
  }
}
